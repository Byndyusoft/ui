{
  "name": "ui-esbuild",
  "version": "1.0.0",
  "main": "index.js",
  "author": "Nick Kuznetsov",
  "license": "MIT",
  "devDependencies": {
    "@web/dev-server": "^0.0.25",
    "@web/dev-server-esbuild": "^0.2.8",
    "esbuild": "^0.8.16"
  },
  "scripts": {
    "start": "wds --node-resolve --watch",
    "dev": "esbuild src/index.tsx --outfile=dist/app.js --bundle --define:process.env.NODE_ENV=\"production\" --serve"
  }
}
