export enum Size {
    Large = 'large',
    Medium = 'medium',
    Small = 'small'
}

export enum Type {
    Button = 'button',
    Submit = 'submit',
    Reset = 'reset'
}

export enum Variant {
    Primary = 'primary',
    Secondary = 'secondary',
    Outline = 'outline',
    Danger = 'danger'
}
