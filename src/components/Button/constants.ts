export enum Size {
    LARGE = 'large',
    MEDIUM = 'medium'
}

export enum Type {
    Button = 'button',
    Submit = 'submit',
    Reset = 'reset'
}

export enum Variant {
    NEUTRAL = 'neutral',
    Default = 'default',
    Primary = 'primary',
    Danger = 'danger'
}

export enum Size {
    Medium = 'medium'
}
