---
to: "<%= hasDocs ? `components/${h.changeCase.paramCase(name)}/src/${name}.docs.mdx` : null %>"
---
import {
    Markdown,
    Source,
    Canvas,
    Meta,
    ArgsTable
} from '@storybook/blocks';
import Readme from '../README.md';
import <%= name %> from './<%= name %>';
import * as <%= name %>Stories from './<%= name %>.stories';

<Meta title="components/<%= name %>" of={<%= name %>Stories} />

<Markdown><Readme /></Markdown>

## Usage

To use the component in your project you must:

1. Import the component where you need it:

<Source
    language="javascript"
    code="import <%= name %> from '@byndyusoft-ui/<%= h.changeCase.paramCase(name) %>';"
/>

2. Call the component and give it the desired props, for example:

<Canvas sourceState="shown" story={<%= name %>Stories.ComponentStory} />

## Props

<ArgsTable of={<%= name %>Stories.ComponentStory} />
