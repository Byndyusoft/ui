---
to: components/<%= h.changeCase.paramCase(name) %>/src/<%= name %>.tsx
---
import React, { FC } from 'react';
import { I<%= name %>Props } from './<%= name %>.types';
import './<%= name %>.css'

const <%= name %>: FC<I<%= name %>Props> = ({ children }): JSX.Element => {
    return (
        <div>{children}</div>
    );
};

export default <%= name %>;
