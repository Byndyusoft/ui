---
to: "<%= hasDocs ? `components/${h.changeCase.paramCase(name)}/src/${name}.stories.mdx` : null %>"
---
import {
  Meta,
  Story,
  Canvas,
  Source,
  ArgsTable
} from '@storybook/addon-docs/blocks';
import <%= name %> from './<%= name %>';

<Meta title="components/<%= name %>" component={<%= name %>} />

# <%= name %>

> Add description

## Usage

To use the component in your project you must:

1. Import the component where you need it:

<Source
  language="javascript"
  code="import { <%= name %> } from '@byndyusoft-ui/<%= h.changeCase.paramCase(name) %>>';"
/>

2. Call the component and give it the desired props, for example:

<Canvas withSource="open">
  <Story id="<%= name.toLowerCase() %>--<%= name.toLowerCase() %>-story" />
</Canvas>


## Props

<ArgsTable of={<%= name %>} />
