---
to: "<%= hasDocs ? `components/${folderName}/src/${name}.stories.mdx` : null %>"
---
import {
    Meta,
    Story,
    Canvas,
    Source,
    ArgsTable,
} from '@storybook/addon-docs/blocks';
import Readme from '../README.md';
import <%= name %> from './<%= name %>';
import { <%= name %>Story } from './<%= name %>.stories';

<Meta title="<%= name %>" component={<%= name %>} />

<Readme />

## Usage

To use the component in your project you must:

1. Import the component where you need it:

<Source
    language="javascript"
    code="import { <%= name %> } from '@byndyusoft/ui';"
/>

2. Call the component and give it the desired props, for example:

<Canvas withSource="open">
    <Story story={<%= name %>Story} />
</Canvas>


## Props

<ArgsTable of={<%= name %>} />
