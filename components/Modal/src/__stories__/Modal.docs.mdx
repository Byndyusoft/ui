import { Meta, Markdown, Canvas, Source, ArgsTable } from '@storybook/blocks';
import Readme from '../../README.md';
import Modal from '../Modal';
import * as ModalStories from './Modal.stories';

<Meta title="components/Modal" of={ModalStories} />

<Markdown>{Readme}</Markdown>

## Usage

To use the component in your project you must:

1. Import the component where you need it:

<Source language="javascript" code="import Modal from '@byndyusoft-ui/Modal';" />

2. Call the component and give it the desired props, for example:

-   Modal with short body

<Canvas sourceState="shown" of={ModalStories.ShortBodyStory} />

-   Modal with long body

<Canvas sourceState="shown" of={ModalStories.LongBodyStory} />
