export enum Size {
    Large = 'large',
    Medium = 'medium',
    Small = 'small'
}

export enum Type {
    Button = 'button',
    Submit = 'submit',
    Reset = 'reset'
}

export enum Variant {
    Default = 'default',
    Primary = 'primary',
    Danger = 'danger',
    Neutral = 'neutral'
}
