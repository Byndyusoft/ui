import { Meta } from '@storybook/addon-docs';
import Readme from '../README.md';
import { Markdown, Source, Canvas, Story} from '@storybook/blocks';
import { TestStory } from './useDebouncedValue.stories';

<Meta title="hooks/useDebouncedValue" />

<Markdown>{Readme}</Markdown>

## Usage

To use the hook in your project you must:

1. Import the component where you need it:


<Source language="javascript" code="import useDebouncedValue from '@byndyusoft-ui/use-debounced-value';" />


2. Give start value and delay props:


<Canvas withSource="open" of={TestStory}>
    <Story story={TestStory} />
</Canvas>
